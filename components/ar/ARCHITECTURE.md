# 🏗️ AR Architecture & Flow

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                    FurniMart Mobile App                      │
│                     (React Native + Expo)                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Product    │    │   AR Demo    │    │  AR Button   │
│   Detail     │    │   Screen     │    │  Component   │
│   Screen     │    │              │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                    ┌──────────────┐
                    │  ARViewer    │
                    │  Component   │
                    └──────────────┘
                              │
                              ▼
                    ┌──────────────┐
                    │   WebView    │
                    │ (react-native│
                    │  -webview)   │
                    └──────────────┘
                              │
                              ▼
                    ┌──────────────┐
                    │  HTML + JS   │
                    │ (model-viewer│
                    │   + WebXR)   │
                    └──────────────┘
                              │
                              ▼
                    ┌──────────────┐
                    │  3D Model    │
                    │   (.glb)     │
                    │              │
                    │  Hosted on:  │
                    │ • GitHub     │
                    │ • Cloudinary │
                    │ • Firebase   │
                    └──────────────┘
```

## 🔄 User Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 1. User opens Product Detail Page                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. App checks if product has 3D model                       │
│    • If yes: Show "XEM TRONG PHÒNG" button                  │
│    • If no: Only show 2D images                             │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. User taps "XEM TRONG PHÒNG"                              │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Modal opens with ARViewer                                │
│    • WebView loads HTML content                             │
│    • model-viewer initializes                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 5. Loading state shows while model downloads                │
│    • Spinner animation                                       │
│    • "Đang tải mô hình 3D..." message                       │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 6. Model loads successfully                                 │
│    • 3D model appears                                        │
│    • Auto-rotation starts                                    │
│    • Controls are active                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 7. User interacts with model                                │
│    • Drag to rotate                                          │
│    • Pinch to zoom                                           │
│    • Tap controls to reset/pause                            │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 8. User taps "Xem AR" button (optional)                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 9. AR mode activates                                        │
│    • Camera opens                                            │
│    • Device scans for surfaces                              │
│    • User taps to place model                               │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 10. Model placed in real space                              │
│     • User can move around                                   │
│     • Model stays in place                                   │
│     • User can resize/rotate                                │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 11. User closes AR viewer                                   │
│     • Tap X button                                           │
│     • Returns to product page                               │
└─────────────────────────────────────────────────────────────┘
```

## 🔧 Component Hierarchy

```
App
│
├── (product)/[id].tsx
│   ├── Product Images
│   ├── Product Info
│   └── ARButton ──────────┐
│                           │
├── ar-demo.tsx             │
│   ├── Product List        │
│   └── ARButton ──────────┤
│                           │
└── components/ar/          │
    │                       │
    ├── ARButton.tsx ◄──────┘
    │   ├── TouchableOpacity
    │   ├── LinearGradient
    │   ├── Icon
    │   ├── Text
    │   └── Modal
    │       └── ARViewer ────┐
    │                        │
    └── ARViewer.tsx ◄───────┘
        ├── WebView
        │   └── HTML Content
        │       ├── model-viewer
        │       ├── Controls
        │       └── Scripts
        └── Close Button
```

## 📦 Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│                      Backend API                             │
│  Returns product data with models3D array                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Product Detail Screen                     │
│  Receives: {                                                 │
│    productColors: [{                                         │
│      models3D: [{                                            │
│        modelUrl: "https://...",                              │
│        previewImage: "https://..."                           │
│      }]                                                      │
│    }]                                                        │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       ARButton                               │
│  Props: {                                                    │
│    modelUrl: string                                          │
│    buttonText?: string                                       │
│    size?: 'small' | 'medium' | 'large'                      │
│    variant?: 'gradient' | 'outline' | 'solid'               │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       ARViewer                               │
│  Props: {                                                    │
│    modelUrl: string                                          │
│    onClose: () => void                                       │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      WebView HTML                            │
│  Embeds modelUrl in <model-viewer src="${modelUrl}">        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    Model-Viewer Library                      │
│  Fetches .glb file from URL                                  │
│  Renders 3D model                                            │
│  Handles AR mode                                             │
└─────────────────────────────────────────────────────────────┘
```

## 🎯 State Management

```
ARButton Component
├── State: showAR (boolean)
│   ├── false: Modal closed
│   └── true: Modal open → renders ARViewer
│
└── Events:
    ├── onPress: setShowAR(true)
    └── onClose: setShowAR(false)

ARViewer Component
├── Props:
│   ├── modelUrl: string
│   └── onClose: function
│
└── WebView:
    ├── HTML content (static)
    └── Dynamic modelUrl injection
```

## 🌐 Network Flow

```
┌─────────────┐
│   Mobile    │
│   Device    │
└─────────────┘
       │
       │ 1. Request product data
       ▼
┌─────────────┐
│  Backend    │
│    API      │
└─────────────┘
       │
       │ 2. Returns product with modelUrl
       ▼
┌─────────────┐
│   Mobile    │
│   Device    │
└─────────────┘
       │
       │ 3. User opens AR
       ▼
┌─────────────┐
│  WebView    │
│   Loads     │
└─────────────┘
       │
       │ 4. Fetch .glb file
       ▼
┌─────────────┐
│   CDN/      │
│  Hosting    │
│  (GitHub/   │
│ Cloudinary) │
└─────────────┘
       │
       │ 5. Download .glb
       ▼
┌─────────────┐
│  WebView    │
│  Renders    │
│   Model     │
└─────────────┘
```

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────────┐
│ 1. App requests product data                                │
│    • Authenticated API call                                  │
│    • JWT token in headers                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Backend validates token                                  │
│    • Checks user permissions                                 │
│    • Returns product data                                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. App receives modelUrl                                    │
│    • Public URL (no auth needed)                             │
│    • Or signed URL (with expiration)                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. WebView fetches .glb                                     │
│    • Direct download                                         │
│    • CORS headers validated                                  │
└─────────────────────────────────────────────────────────────┘
```

## 📱 Platform-Specific Flow

### iOS Flow
```
User taps "Xem AR"
    │
    ▼
WebXR checks for AR support
    │
    ▼
iOS: Uses AR Quick Look
    │
    ├─ ARKit initializes
    ├─ Camera activates
    ├─ Plane detection starts
    └─ Model ready to place
```

### Android Flow
```
User taps "Xem AR"
    │
    ▼
WebXR checks for AR support
    │
    ▼
Android: Uses Scene Viewer
    │
    ├─ ARCore initializes (if available)
    ├─ Camera activates
    ├─ Plane detection starts
    └─ Model ready to place
```

## 🎨 Rendering Pipeline

```
.glb file
    │
    ▼
model-viewer loads
    │
    ├─ Parse glTF structure
    ├─ Load textures
    ├─ Load geometry
    └─ Load materials
    │
    ▼
Three.js renderer
    │
    ├─ Create scene
    ├─ Add lights
    ├─ Add camera
    └─ Add model
    │
    ▼
WebGL rendering
    │
    ├─ Vertex shader
    ├─ Fragment shader
    └─ Rasterization
    │
    ▼
Display on screen
```

## 🔄 Lifecycle

```
Component Mount
    │
    ▼
ARButton renders
    │
    ▼
User taps button
    │
    ▼
Modal opens
    │
    ▼
ARViewer mounts
    │
    ▼
WebView initializes
    │
    ▼
HTML content loads
    │
    ▼
model-viewer script loads
    │
    ▼
Model starts downloading
    │
    ▼
Loading state shows
    │
    ▼
Model loads
    │
    ▼
Render complete
    │
    ▼
User interacts
    │
    ▼
User closes
    │
    ▼
ARViewer unmounts
    │
    ▼
Modal closes
    │
    ▼
ARButton remains
```

## 💾 Caching Strategy

```
First Load:
    Download .glb → Cache in memory → Display

Subsequent Loads:
    Check cache → If exists: Display
                → If not: Download → Cache → Display

Cache Invalidation:
    • On app restart
    • On memory warning
    • Manual clear
```

## 🎯 Performance Optimization

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Lazy Loading                                             │
│    • ARViewer only loads when modal opens                   │
│    • model-viewer script loads on demand                    │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 2. Model Optimization                                       │
│    • Draco compression                                       │
│    • Texture compression                                     │
│    • LOD (Level of Detail)                                  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 3. Network Optimization                                     │
│    • CDN delivery                                            │
│    • Preload hints                                           │
│    • Progressive loading                                     │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│ 4. Rendering Optimization                                   │
│    • WebGL hardware acceleration                            │
│    • Frustum culling                                         │
│    • Texture atlasing                                        │
└─────────────────────────────────────────────────────────────┘
```

---

**Architecture Version**: 1.0.0
**Last Updated**: 2025-12-05
